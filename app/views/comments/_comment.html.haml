- if comment.id
    .comment
        %p= "Posted by#{comment.user.email}"
        %p= "Written #{time_ago_in_words(comment.created_at)} minutes ago"
        - if current_user == comment.user
            .button
                = button_to "Delete", [post,comment], method: :delete
        %hr
        %p= comment.comment
    